# Enforce PNPM usage for this monorepo
engine-strict=true

# Prevent npm and yarn from being used
# Users should use pnpm instead

# Helpful error message for wrong package manager usage
# If you see this error, please use PNPM:
# - To install all dependencies: pnpm install
# - To add a dependency to a workspace: pnpm add <package> --filter <workspace>
# - To run scripts: pnpm run <script> --filter <workspace>

# Available workspaces:
# - @safestep/mobile (packages/mobile)
# - @safestep/api (packages/api)  
# - @safestep/shared (packages/shared)